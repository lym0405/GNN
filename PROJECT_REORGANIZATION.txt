╔══════════════════════════════════════════════════════════════════════╗
║                    프로젝트 구조 재정리 완료                         ║
║                         2026년 1월 19일                              ║
╚══════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────┐
│ 1. 정리 개요                                                         │
└──────────────────────────────────────────────────────────────────────┘

✅ Phase별 폴더 분리 완료
✅ 모든 파일 경로 수정 완료
✅ 테스트 실행 확인 완료

기존 구조:
  GNN/
  ├── src/           # 모든 Phase 코드 혼재
  ├── main_phase*.py # 메인 파일들 루트에 산재
  └── ...

새로운 구조:
  GNN/
  ├── phase1/        # Phase 1 전용
  ├── phase2/        # Phase 2 전용
  ├── phase3/        # Phase 3 전용
  ├── data/          # 공통 데이터
  └── results/       # 공통 결과

┌──────────────────────────────────────────────────────────────────────┐
│ 2. Phase 1: 생산 레시피 추정                                         │
└──────────────────────────────────────────────────────────────────────┘

phase1/
├── src/
│   ├── b_matrix_generator.py      # B 행렬 생성기
│   ├── inventory_module.py        # 레시피 추정 모듈
│   ├── check_recipe.py            # 레시피 검증
│   └── debug_deep_dive.py         # 상세 분석 도구
│
├── main_phase1.py                 # 메인 실행 파일
├── generate_dummy_data.py         # 테스트 데이터 생성
├── quick_test.sh                  # 빠른 테스트 스크립트
├── requirements.txt               # 패키지 목록
├── README.md                      # 상세 문서
└── STRUCTURE.txt                  # 구조 설명

실행 방법:
  $ cd phase1
  $ bash quick_test.sh              # 빠른 테스트
  $ python main_phase1.py           # 전체 실행

출력:
  ../data/processed/disentangled_recipes.pkl  # 기업별 33차원 레시피

┌──────────────────────────────────────────────────────────────────────┐
│ 3. Phase 2: GraphSAGE 정적 임베딩                                    │
└──────────────────────────────────────────────────────────────────────┘

phase2/
├── src/
│   ├── graph_builder.py           # 그래프 구축
│   ├── sampler.py                 # 커리큘럼 네거티브 샘플러
│   ├── GraphSAGE.py               # GraphSAGE 모델
│   ├── loss.py                    # Risk-aware BCE Loss
│   └── trainer.py                 # 커리큘럼 트레이너
│
├── main_phase2.py                 # 메인 실행 파일
├── generate_phase2_dummy_data.py  # 테스트 데이터 생성
├── quick_test_phase2.sh           # 빠른 테스트 스크립트
├── test_phase2.py                 # 테스트 스크립트
├── requirements.txt               # 패키지 목록
├── README.md                      # 상세 문서
└── STRUCTURE.txt                  # 구조 설명

실행 방법:
  $ cd phase2
  $ bash quick_test_phase2.sh       # 빠른 테스트
  $ python main_phase2.py           # 전체 실행

출력:
  ../data/processed/node_embeddings_static.pt  # 노드 임베딩
  ../data/processed/train_edges.npy            # 학습용 엣지
  ../data/processed/test_edges.npy             # 테스트용 엣지

┌──────────────────────────────────────────────────────────────────────┐
│ 4. Phase 3: 하이브리드 링크 예측                                     │
└──────────────────────────────────────────────────────────────────────┘

phase3/
├── src/
│   ├── temporal_graph_builder.py  # 시계열 그래프 구축
│   ├── sc_tgn.py                  # SC-TGN 모델 (Track A)
│   ├── graphseal.py               # GraphSEAL 모델 (Track B)
│   ├── hybrid_trainer.py          # 하이브리드 트레이너
│   ├── loss.py                    # TIS-aware BCE + Ranking Loss
│   ├── negative_sampler.py        # 네거티브 샘플러
│   ├── benchmarks.py              # 벤치마크 (PA, RA, JC)
│   ├── metrics.py                 # 평가 지표
│   ├── robustness_test.py         # 견고성 테스트
│   ├── link_predictor.py          # 링크 예측기
│   └── trainer_alt.py             # 대체 트레이너
│
├── main.py                        # 메인 실행 파일 (100 epochs)
├── quick_test.py                  # 빠른 테스트 (5 epochs)
├── test.py                        # 테스트 스크립트
├── evaluate_comprehensive.py      # 종합 평가 (벤치마크 비교)
├── generate_temporal_networks.py  # 시계열 네트워크 생성
├── requirements.txt               # 패키지 목록
├── README.md                      # 상세 문서
├── STRUCTURE.txt                  # 구조 설명
└── FINAL_SUMMARY.txt              # 최종 요약

실행 방법:
  $ cd phase3
  $ python quick_test.py            # 빠른 테스트 (5 epochs)
  $ python main.py                  # 전체 학습 (100 epochs)
  $ python evaluate_comprehensive.py  # 종합 평가

출력:
  ../results/quick_test/            # 빠른 테스트 결과
  ../results/phase3/                # 전체 학습 결과

┌──────────────────────────────────────────────────────────────────────┐
│ 5. 공통 디렉토리                                                     │
└──────────────────────────────────────────────────────────────────────┘

data/
├── raw/                            # 원본 데이터
│   ├── A_33.csv                    # IO 테이블
│   ├── H_csr_model2.npz            # 거래 네트워크
│   ├── firm_to_idx_model2.csv      # 기업 인덱스
│   ├── posco_network_20XX.csv      # 연도별 네트워크
│   └── ...
│
└── processed/                      # 처리된 데이터
    ├── disentangled_recipes.pkl    # Phase 1 출력
    ├── node_embeddings_static.pt   # Phase 2 출력
    ├── train_edges.npy             # Phase 2 출력
    └── test_edges.npy              # Phase 2 출력

results/
├── quick_test/                     # Phase 3 빠른 테스트 결과
│   ├── hybrid_model_quick.pt
│   └── phase3_metrics_quick.npz
└── phase3/                         # Phase 3 전체 학습 결과

┌──────────────────────────────────────────────────────────────────────┐
│ 6. 주요 변경 사항                                                    │
└──────────────────────────────────────────────────────────────────────┘

📦 파일 이동:
  ✅ src/ → phase1/src/, phase2/src/, phase3/src/
  ✅ main_phase*.py → phase*/main*.py
  ✅ requirements_phase*.txt → phase*/requirements.txt
  ✅ PHASE*_README.md → phase*/README.md
  ✅ PHASE*_STRUCTURE.txt → phase*/STRUCTURE.txt

🔧 경로 수정:
  ✅ 모든 import 경로: src.xxx → phaseX.src.xxx
  ✅ 데이터 경로: data/ → ../data/
  ✅ 결과 경로: results/ → ../results/
  ✅ sys.path 설정: PROJECT_ROOT 추가

📝 새 파일:
  ✅ README.md (루트 프로젝트 전체 설명)

✅ 테스트 확인:
  ✅ phase3/quick_test.py 실행 성공
  ✅ 모든 import 오류 해결
  ✅ 경로 문제 해결

┌──────────────────────────────────────────────────────────────────────┐
│ 7. 실행 가이드                                                       │
└──────────────────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ 전체 파이프라인 실행 (처음부터 끝까지)                              ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# 1. Phase 1: 레시피 추정
cd phase1
python main_phase1.py

# 2. Phase 2: GraphSAGE 임베딩
cd ../phase2
python main_phase2.py

# 3. Phase 3: 링크 예측
cd ../phase3
python main.py

# 4. 종합 평가
python evaluate_comprehensive.py

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ 빠른 테스트 (각 Phase별)                                             ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# Phase 1 (1,000개 기업)
cd phase1
bash quick_test.sh

# Phase 2 (500개 기업, 10 epochs)
cd ../phase2
bash quick_test_phase2.sh

# Phase 3 (500개 기업, 5 epochs)
cd ../phase3
python quick_test.py

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ 개별 Phase 실행                                                      ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# Phase 1만 실행
cd phase1
python main_phase1.py

# Phase 2만 실행 (Phase 1 결과 필요)
cd phase2
python main_phase2.py

# Phase 3만 실행 (Phase 1, 2 결과 필요)
cd phase3
python main.py

┌──────────────────────────────────────────────────────────────────────┐
│ 8. 폴더 구조 요약                                                    │
└──────────────────────────────────────────────────────────────────────┘

GNN/                               # 프로젝트 루트
├── README.md                      # 📖 전체 프로젝트 가이드
│
├── phase1/                        # 🔹 Phase 1 모듈
│   ├── src/                       # Phase 1 코드
│   ├── main_phase1.py
│   └── ...
│
├── phase2/                        # 🔹 Phase 2 모듈
│   ├── src/                       # Phase 2 코드
│   ├── main_phase2.py
│   └── ...
│
├── phase3/                        # 🔹 Phase 3 모듈
│   ├── src/                       # Phase 3 코드
│   ├── main.py
│   ├── quick_test.py
│   ├── evaluate_comprehensive.py
│   └── ...
│
├── data/                          # 📊 공통 데이터
│   ├── raw/                       # 원본 데이터
│   └── processed/                 # 처리된 데이터
│
└── results/                       # 📈 공통 결과
    ├── quick_test/                # 빠른 테스트 결과
    └── phase3/                    # Phase 3 결과

┌──────────────────────────────────────────────────────────────────────┐
│ 9. 장점 및 개선 사항                                                 │
└──────────────────────────────────────────────────────────────────────┘

✅ 장점:
  1. Phase별 독립성: 각 Phase가 독립적인 모듈로 분리
  2. 명확한 구조: 파일 위치가 직관적
  3. 유지보수 용이: 각 Phase별로 관리 가능
  4. 확장성: 새로운 Phase 추가 쉬움
  5. 테스트 편리: Phase별 개별 테스트 가능

🎯 개선 사항:
  1. src/ 폴더 정리 완료 (Phase별 분리)
  2. main 파일 정리 완료 (Phase별 폴더로 이동)
  3. 경로 일관성 확보 (모두 상대 경로로 통일)
  4. import 구조 개선 (phaseX.src.xxx)
  5. 문서화 강화 (루트 README.md 추가)

┌──────────────────────────────────────────────────────────────────────┐
│ 10. 다음 단계                                                        │
└──────────────────────────────────────────────────────────────────────┘

✅ 정리 완료:
  - Phase별 폴더 분리
  - 경로 수정
  - 테스트 확인

🚀 다음 작업:
  1. 전체 파이프라인 실행 (phase1 → phase2 → phase3)
  2. 하이퍼파라미터 튜닝
  3. 실제 데이터로 실험
  4. 결과 분석 및 시각화
  5. 논문/보고서 작성

╔══════════════════════════════════════════════════════════════════════╗
║                          정리 완료! 🎉                               ║
║                 깔끔한 구조로 프로젝트 개선 완료                      ║
╚══════════════════════════════════════════════════════════════════════╝
